import ProductCard from "../../components/ProductCard"
import stories from "../../data/stories.json"

export default function Page() {
  return (
    <section>
      <h1>Firefly Hollow Stories</h1>
      {stories.map((item) => (
        <ProductCard key={item.id} item={item} />
      ))}
    </section>
  )
}
