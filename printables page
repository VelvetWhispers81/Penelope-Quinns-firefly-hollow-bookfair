import ProductCard from "../../components/ProductCard"
import data from "../../data/printables.json"

export default function Page() {
  return (
    <section>
      <h1>Printables</h1>
      {data.map(item => <ProductCard key={item.id} item={item} />)}
    </section>
  )
}
